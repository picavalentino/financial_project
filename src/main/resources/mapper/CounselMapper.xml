<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.team.financial_project.counsel.mapper.CounselMapper">

    <!--  MyBatis Test  -->
    <select id="checkConnection" resultType="int">
        SELECT 1
    </select>

    <!-- 상담 목록 페이징 -->
    <select id="getPagedCounselData" parameterType="java.util.Map" resultType="com.team.financial_project.counsel.dto.TbCounselDTO">
        SELECT * FROM TB_COUNSEL ORDER BY tb_counsel.counsel_id ASC LIMIT #{limit} OFFSET #{offset}
    </select>

</mapper>